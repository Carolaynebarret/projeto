# Imagens de documentação

Esta pasta ainda não contém capturas de tela. Para enriquecer o `README.md` principal, seria interessante adicionar aqui:

- `home.png` — tela inicial (`/`), com a chamada para o formulário.
- `formulario.png` — tela do formulário de dados pessoais (`/Formulario`).
- `dashboard.png` — tela de gráficos (`/dashboard`), mostrando o gráfico de pizza e o gráfico de barras lado a lado.
- `contatos.png` — tela de contatos (`/contatos`), com os links da equipe.

Ao adicionar uma imagem aqui, referencie-a no `README.md` da raiz do projeto usando um caminho relativo, por exemplo:

```markdown
![Dashboard](docs/images/dashboard.png)
```
